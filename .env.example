# Anthropic API key used by /api/poem to generate poetic fragments.
# Without it the site falls back to the built-in fragments (no error shown).
ANTHROPIC_API_KEY=

# Canonical site URL used for metadata, sitemap and robots.
NEXT_PUBLIC_SITE_URL=https://mobenet.vercel.app
